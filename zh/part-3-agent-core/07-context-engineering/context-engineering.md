# 07. Context Engineering

这一章现在作为总章使用。

这里把原来分开的 `Context Engineering` 和 `Context Management` 合并理解：

- `Context Engineering` 是总问题
- `Context Management` 是其中最核心的一部分

可以顺着下面的子文章继续读：

- [Context Management](./context-management.md)

本章会讨论如何设计更适合模型推理的上下文结构。
